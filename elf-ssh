#!/bin/bash

# Help message
helpmsg=\
"Usage: elf-ssh [arguments]

Where [arguments] can be:
	home         - ssh to your home workstation
";
if [ $# -eq 0 ]; then
	echo "$helpmsg"
	exit 0;
fi

# Actions
case $1 in
	home)
		ssh mathjs@`elf-ip home` ;;
esac;
